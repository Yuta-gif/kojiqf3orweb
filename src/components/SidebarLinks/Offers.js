//求人作成画面
/*
サイドバーやフッターは他のページと共通
デフォルトの状態では作成した求人
が表示されるだけ。求人を作成したいのであれば
さらにこの画面上にある求人作成
というボタンをタップする必要があり
*/

import React from 'react';
import Sidebar from '../UI/Sidebar';
import MainContent from '../UI/MainContent';


const Offers=()=>{
    return (
        <div>

        </div>
    );
};

export default Offers;
